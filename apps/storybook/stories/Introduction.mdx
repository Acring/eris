import { Meta } from '@storybook/blocks';
import Image from 'next/image';

import xsky from './assets/xsky.png';

<Meta title="Introduction" />

# Welcome to Eris UI

<div className="mb-4 flex items-center justify-center rounded-lg bg-primary-normal p-2">
  <Image src={xsky} alt="xsky" />
</div>

Eris UI 组件库是一个基于 React + tailwind + Radix 的组件库，它的目标是提供一套简单易用的组件，让 XSKY 开发者可以快速的构建出业务功能，并统一 XSKY 的产品风格。
